// const fs = require('fs');
// const path = require('path');

// const rootFolder = path.join (__dirname, '..', 'Movies');

// if (!fs.existsSync(rootFolder)) {
//   fs.mkdirSync(rootFolder);
// }

// const movieNames = Array.from({length: 15}, (_,i) => `Movie ${i + 1}`)

// movieNames.forEach((movieName) => {
//   const movieFolder = path.join(rootFolder, movieName);
  
//   if(!fs.existsSync(movieFolder)) {
//     fs.mkdirSync(movieFolder);
//   }


// })
